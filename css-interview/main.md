## Box Modal

box-sizing: content-box;   /* total width = width + padding + border */

box-sizing: border-box;   /* total width = width (includes padding + border) */

pseudo element= *, *::before, *::after  apply to all elements */

